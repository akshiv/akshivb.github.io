source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# No longer bundled with Ruby by default (Ruby 3.4+); github-pages' pinned
# Jekyll 3.9 still needs these at runtime.
gem "csv"
gem "base64"
gem "logger"
gem "webrick"
